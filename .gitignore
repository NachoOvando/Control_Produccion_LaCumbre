# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — NUNCA commitear credenciales
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/migrations/**/migration_lock.toml

# graphify — grafo de conocimiento local, regenerable (graphify update .)
/graphify-out/

# backups de DB — datos reales, nunca al repo
/backups/

# service worker generado por serwist en cada build (swDest en next.config.mjs).
# Es un artefacto de build con el manifest de precache hasheado: commitearlo
# generaría conflictos en cada merge y quedaría desalineado del bundle real.
# La FUENTE del SW es src/app/sw.ts, y esa sí va al repo.
/public/sw.js
/public/sw.js.map
/public/swe-worker-*.js
